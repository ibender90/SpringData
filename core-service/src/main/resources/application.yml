spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_secure_db
    username: postgres
    password: kolbaska
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    serialization:
      indent_output: true

server:
  port: 8181
  servlet:
      context-path: /market-core

integrations:
  cart-service:
    url: http://localhost:8182/market-carts
    connect-timeout: 5000
    read-timeout: 10000
    write-timeout: 5000
